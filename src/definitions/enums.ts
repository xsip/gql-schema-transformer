export enum GqlFunctionType {
    Query,
    Mutation
}
// values need to be in lower case since the comparison also will be with lowercase values!
export enum DefaultGqlTypes {
    Int = 'int',
    Float = 'float',
    String = 'string',
    Boolean = 'boolean'
}